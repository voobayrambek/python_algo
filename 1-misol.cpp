#include<iostream>
using namespace std;
double D(double q, double p){
    return L=pow(q,p);
}
int main(){
    double q,p;
    cout<<"Sonni kiriting.";
    cin>>q>>p;
    double L=D(q,p);
    cout<<L;
}
